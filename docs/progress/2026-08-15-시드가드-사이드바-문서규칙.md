# 2026-08-15 — 시드 가드·사이드바·README·설계 문서

## 결과

파일 H2에서 시드 계정이 하나라도 있으면 DemoData를 건너뛴다. 사이드바는 동작하는 화면만 남긴다. 루트 README는 전체 소개, 실행 상세는 각 폴더 README. 계정·역할 설계를 `decisions/`에 적고, 하루 끝에 문서를 같은 날 갱신하는 규칙을 INDEX에 명시한다.

## 작업한 파일

- `DemoDataConfig.java` — `member01`·`trainer01`·`doctor01`·`admin01` 중 하나라도 있으면 `return`
- `MemberDashboard.jsx` / `ProfessionalDashboard.jsx` — 준비 중 메뉴 제거. checkJs용 CardHead·ProgressBar JSDoc
- `frontend/.env.example` — `8081`, `VITE_USE_MOCK=false`
- 루트 `README.md` — 폴더 링크 중심. 역할별 시연 표. 실행 절차는 `frontend`/`backend`/`ai-service` README
- `backend/README.md` — 파일 H2, `SERVER_PORT=8081`
- `docs/decisions/계정-역할-소유권.md`
- `docs/study/codeReview/session-권한-rag-흐름.md` (구현 후. step4 구현 전 문서는 유지)
- `docs/INDEX.md` — 하루 마감 규칙

## 설계 결정

- 일반 회원 「확인 필요」는 `MemberStatus` 기본값. 관리자 승인이 아님. 목록에 보이면 DB에 저장된 것
- 루트 README에 데모 비밀번호·긴 실행 로그를 중복하지 않음

## 진행 중 겪은 문제

1. 시드 가드 `if` 괄호를 `member01`에서 닫고 `{` 안에 `||`를 넣음 → 컴파일 실패. 조건은 `if (` `)` 사이, Repository는 `accountRepository`
2. 관리자 화면 「확인 필요」를 미저장으로 오해. 공개 가입 MEMBER는 저장되고 상태는 CHECK_REQUIRED
3. 루트 README에 실행 전체를 넣으면 폴더 README와 겹침 → 루트는 소개+링크

## 다음에 할 일

- [x] 시드 가드·사이드바·README·설계 문서 커밋 (`6c9a01c`)
- [ ] 화면 캡처 또는 짧은 시연 (v1.0)
- [x] PR #4 `dev`→`main` (요청으로 머지)
- [ ] CSRF / Flyway (운영 전, v1.0 이후)
